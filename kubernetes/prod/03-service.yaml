---
apiVersion: v1
kind: Service
metadata:
  labels:
    env: prod
    app: flight-service
    layer: frontend
  name: prod-flight-service-service
  namespace: prod
spec:
  type: NodePort
  selector:
    app: flight-service
  ports:
    - name: api
      port: 8080
      targetPort: 8080