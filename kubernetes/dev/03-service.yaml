---
apiVersion: v1
kind: Service
metadata:
  labels:
    env: dev
    app: flight-service
    layer: frontend
  name: dev-flight-service-service
  namespace: dev
spec:
  type: NodePort
  selector:
    app: flight-service
  ports:
    - name: api
      port: 8080
      targetPort: 8080